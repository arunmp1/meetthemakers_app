<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Requirement Detail</title>
    <style>
        :root {
            --primary-color: #4285F4;
            --accent-color: #FF6F61;
            --text-color: #333;
            --text-secondary: #666;
            --bg-color: #f8f9fa;
            --card-bg: #ffffff;
            --border-radius: 8px;
            --shadow: 0 4px 12px rgba(0, 0, 0, 0.08);
            --transition: all 0.3s ease;
        }

        body {
            font-family: 'Inter', 'Segoe UI', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
        }

        /* Requirement Container */
        .requirement-container {
            padding: 2.5rem;
            max-width: 900px;
            margin: 2.5rem auto;
            background-color: var(--card-bg);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
        }

        .requirement-heading {
            font-size: 2.25rem;
            font-weight: 700;
            margin-bottom: 1.25rem;
            color: var(--text-color);
            line-height: 1.3;
        }

        .requirement-content {
            font-size: 1.125rem;
            line-height: 1.8;
            margin-bottom: 2rem;
            color: var(--text-secondary);
        }

        .requirement-details {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;
            margin-bottom: 2rem;
            background-color: rgba(66, 133, 244, 0.05);
            padding: 1.25rem;
            border-radius: var(--border-radius);
        }

        .detail-item {
            display: flex;
            flex-direction: column;
        }

        .detail-label {
            font-size: 0.875rem;
            color: var(--text-secondary);
            margin-bottom: 0.25rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .detail-value {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-color);
        }

        .user-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.25rem;
            background-color: rgba(0, 0, 0, 0.02);
            border-radius: var(--border-radius);
            margin-bottom: 1.5rem;
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .user-info .user-name {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-color);
            margin-bottom: 0.25rem;
        }

        .user-info .user-email {
            font-size: 0.95rem;
            color: var(--text-secondary);
        }

        .like-button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background-color: var(--primary-color);
            color: white;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            font-weight: 600;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: var(--transition);
            text-decoration: none;
            gap: 0.5rem;
        }

        .like-button:hover {
            background-color: #3367d6;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .unlike-button {
            background-color: #f1f3f4;
            color: var(--text-color);
        }

        .unlike-button:hover {
            background-color: #e4e6e9;
        }

        /* Comment Section */
        .comment-section {
            max-width: 900px;
            margin: 0 auto 4rem;
            padding: 2.5rem;
            background-color: var(--card-bg);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
        }

        .comment-header {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            position: relative;
            display: inline-block;
        }

        .comment-header::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 0;
            width: 40px;
            height: 3px;
            background-color: var(--accent-color);
            border-radius: 2px;
        }

        .comment-form {
            width: 100%;
            margin-bottom: 2rem;
        }

        .comment-form textarea {
            width: 100%;
            padding: 1rem;
            font-size: 1rem;
            margin-bottom: 1rem;
            border-radius: var(--border-radius);
            border: 1px solid rgba(0, 0, 0, 0.1);
            resize: vertical;
            min-height: 120px;
            transition: var(--transition);
            font-family: inherit;
        }

        .comment-form textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(66, 133, 244, 0.2);
        }

        .comment-form button {
            background-color: var(--accent-color);
            color: white;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            font-weight: 500;
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            transition: var(--transition);
        }

        .comment-form button:hover {
            background-color: #e24d3d;
            transform: translateY(-2px);
        }

        .comment-list {
            list-style-type: none;
            padding: 0;
            width: 100%;
        }

        .comment-item {
            background-color: rgba(0, 0, 0, 0.02);
            padding: 1.25rem;
            margin-bottom: 1rem;
            border-radius: var(--border-radius);
            border: 1px solid rgba(0, 0, 0, 0.05);
            transition: var(--transition);
            position: relative;
            /* padding-right: 40px; */
        }

        .comment-item:hover {
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
        }

        .comment-item .comment-author {
            font-weight: 600;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
        }

        .delete-btn{
            background-color: red;
            color: white;
            border: none;
            padding: 5px 10px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 3px;
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
        }

        .delete-btn:hover{
            background-color: darkred;
        }

        .author-name{
            font-weight: bold;
            color:#333;
        }

        .author-email{
            font-size: 14px;
            color: #555;
            margin-left: 10px;
            font-style: italic;
        }

        .comment-author:before {
            content: '';
            display: inline-block;
            width: 24px;
            height: 24px;
            background-color: var(--accent-color);
            border-radius: 50%;
            margin-right: 0.75rem;
            opacity: 0.8;
        }

        .comment-item .comment-body {
            margin-top: 0.5rem;
            font-size: 1rem;
            color: var(--text-secondary);
            padding-left: 2.25rem;
        }

        .no-comments {
            color: var(--text-secondary);
            font-style: italic;
            text-align: center;
            padding: 2rem 0;
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .requirement-container, .comment-section {
                padding: 1.5rem;
                margin: 1.25rem;
            }

            .requirement-heading {
                font-size: 1.75rem;
            }

            .requirement-details {
                grid-template-columns: 1fr;
                gap: 0.75rem;
            }

            .like-button {
                font-size: 0.95rem;
                padding: 0.625rem 1.25rem;
            }

            .comment-form textarea {
                font-size: 0.95rem;
                min-height: 100px;
            }
        }
    </style>
</head>
<body>
<%- include('./partials/navbar') %>

<div class="requirement-container">
    <div class="requirement-heading">
        <%= requirement.heading %>
    </div>
    <div class="requirement-content">
        <%= requirement.content %>
    </div>

    <div class="requirement-details">
        <div class="detail-item">
            <span class="detail-label">Price</span>
            <span class="detail-value">₹<%= requirement.price %></span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Quantity</span>
            <span class="detail-value"><%= requirement.quantity %></span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Likes</span>
            <span class="detail-value"><%= requirement.likes.length %></span>
        </div>
    </div>

    <div class="user-info">
        <div>
            <div class="user-name"><%= requirement.user.name %></div>
            <div class="user-email"><%= requirement.user.email %></div>
        </div>
        <% if (userLiked) { %>
            <a href="/read/like/<%= requirement._id %>" class="like-button unlike-button">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="currentColor" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                </svg>
                Unlike
            </a>
        <% } else { %>
            <a href="/read/like/<%= requirement._id %>" class="like-button">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                </svg>
                Like
            </a>
        <% } %>
    </div>
</div>

<!-- Comment Section -->
<div class="comment-section">
    <div class="comment-header">Comments</div>

    <!-- Comment Form -->
    <form action="/read/comment/<%= requirement._id %>" method="POST" class="comment-form">
        <textarea name="content" placeholder="Share your thoughts..." required></textarea>
        <button type="submit">Post Comment</button>
    </form>

    <!-- Comment List -->
    <ul class="comment-list">
        <% if (comment.length === 0) { %>
            <div class="no-comments">No comments yet. Be the first to comment!</div>
        <% } else { %>
            <% comment.forEach(element => { %>
                <li class="comment-item">
                    <div class="comment-author">
                        <strong class="author-name"><%= element.createdBy.name %></strong>
                        <span class="author-email"><%= element.createdBy.email %></span>
                    </div>
                    <div class="comment-body"><%= element.content %></div>
                    <form action="/read/delete-comment/<%=element._id%>" method="post">

                        <% if(element.createdBy._id.toString() == user._id.toString()){ %>
                            <button class="delete-btn" type="submit">Delete</button>
                            <% }%>
                        </form>
                </li>
            <% }) %>
        <% } %>
    </ul>
</div>

<%- include('./partials/footer') %>

</body>
</html>